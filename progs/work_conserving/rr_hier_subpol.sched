// This policy is just designed to be a sub-policy of rr_hier.sched
// It also swaps the the order of the RP and RT classes to test that the comparison of the children of RR is order-agnostic

classes RP, RT;

policy = rr[RT, RP];

return policy