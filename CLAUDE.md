# Notes for Claude (working in this repo)

## Build / test workflow

The OCaml toolchain (`opam`, `dune`) is available in the sandbox.
After a code change, you can run `dune test` from
`rio/` yourself instead of handing the diff back to Anshuman.
